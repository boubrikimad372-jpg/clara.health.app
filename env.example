# ──────────────────────────────────────────────────────────────────────────────
# Clara Health App — Environment Variables
# ──────────────────────────────────────────────────────────────────────────────
#
# LOCAL DEVELOPMENT:
#   1. Copy this file to .env  →  cp .env.example .env
#   2. Fill in your key below (get it free from console.groq.com)
#   3. Run: vercel dev   (NOT vite dev — needed to run /api functions locally)
#
# VERCEL PRODUCTION:
#   Add GROQ_API_KEY in:
#   Vercel Dashboard → Your Project → Settings → Environment Variables
#   ⚠️  Use exactly "GROQ_API_KEY" — no VITE_ prefix
#   ⚠️  Redeploy after adding/changing the variable
#
# GET YOUR FREE KEY:
#   → https://console.groq.com  (no credit card required)
#
# ──────────────────────────────────────────────────────────────────────────────

GROQ_API_KEY=your_groq_api_key_here
